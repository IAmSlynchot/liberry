/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "liberry-image-storage-bucket201942-staging.s3.us-east-1.amazonaws.com"],
      },
}

module.exports = nextConfig
